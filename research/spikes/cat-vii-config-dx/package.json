{
  "name": "spike-cat-vii-config-dx",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Cat VII spike — config/code boundary, definePipeline() DX, CLI surface, describe() introspection. Throwaway.",
  "scripts": {
    "spike:config-boundary": "bun run src/probe-config-boundary.ts",
    "spike:cli-and-templates": "bun run src/probe-cli-and-templates.ts",
    "spike:describe-introspection": "bun run src/probe-describe-introspection.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "zod": "^3.23.0"
  }
}

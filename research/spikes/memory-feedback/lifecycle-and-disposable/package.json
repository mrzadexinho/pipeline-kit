{
  "name": "spike-cat-v-lifecycle-and-disposable",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Cat V spike #2 — where does adapter lifecycle (close/initialize) live in pipeline-kit's type system? 3 cells: alpha (close on contract), beta (Disposable opt-in), gamma (Composer-owned lifetime). Throwaway.",
  "scripts": {
    "spike": "bash run-spike-2.sh",
    "alpha": "bun run cell-alpha-close-on-contract.ts",
    "beta": "bun run cell-beta-disposable-opt-in.ts",
    "gamma": "bun run cell-gamma-composer-owned-lifetime.ts"
  },
  "dependencies": {
    "orchestr8-mcp": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
